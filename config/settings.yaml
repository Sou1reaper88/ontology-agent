# 配置默认值（敏感项通过 .env 覆盖，勿提交真实密钥）
app_name: ontology-agent
env: dev
debug: true

database:
  host: localhost
  port: 5432
  user: ontology
  password: ontology
  name: ontology_agent

redis:
  host: localhost
  port: 6379
  db: 0

hive:
  host: localhost
  port: 10000
  user: hive
  database: default
  timeout_seconds: 300

# LLM 与本体平台配置请通过 .env 设置（敏感/环境相关，见 .env.example）
