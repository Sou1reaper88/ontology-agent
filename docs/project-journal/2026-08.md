# 2026 年 8 月工程日志

## 2026-08-21：Python 本体内核重构设计

### 背景

现有项目能够读取数据库表结构、字段注释、关系和逻辑定义，并通过 LangGraph 编排生成 HiveSQL。但业务概念与物理表绑定，本体能力主要表现为增强型元数据层，缺少正式 RDF/OWL 模型和可执行约束。

### 难点与痛点

- `DbOntologyClient` 同时承担元数据读取、本体检索、字段映射和 SQL 校验，职责边界过宽。
- 本体对象基本等同于物理表，未来接入其他数据库和编译器时容易重复建模。
- 直接引入 Java 本体服务会扩大当前 Python 团队的技术栈和部署复杂度。
- 真实电信表名、字段和口径不适合硬编码进生产代码或公开测试夹具。

### 方案取舍

评估了三种方向：

1. Python 原生 RDFLib + pySHACL。
2. 保持 MySQL 元数据为事实源，仅导出 TTL。
3. 以 GraphRAG 为核心重建语义检索。

选择第一种方向。原因是它能够建立正式 RDF/OWL 与 SHACL 边界，同时保留 Python、FastAPI 和 LangGraph 技术栈；相比 MySQL-first 方案，业务语义可以脱离物理表；相比 GraphRAG，正式本体更符合结构化查询和多编译器目标。

### 解决方式

- 设计独立 `OntologyService`，拆分 Repository、Validator、Resolver、MappingRegistry 和 QueryPlanValidator。
- 使用版本化本体包作为事实源，MySQL 只作为候选数据导入源。
- 同时提供 Python SDK 和 REST API，并共享 Pydantic 契约。
- 首期旁路接入，不改动现有 Agent 和 HiveSQL 链路。
- 测试使用匿名中性概念；生产代码不硬编码真实业务字段。

### 验证证据

- 设计文档提交：`1dd3541 docs: 设计 Python 本体内核首期重构`。
- 设计范围已经确认：首期只建立正式本体层，后续再改造数据库适配和编译器。
- 当前状态：设计完成，尚未进入实现，不将计划能力表述为已交付成果。

### 可复用经验

在数据分析系统向 Agent 系统演进时，应先把业务语义、物理数据映射和查询编译拆成独立边界。LLM 适合负责理解和规划，正式本体负责概念与约束，编译器负责生成目标平台查询，执行器负责访问数据源。

## 2026-08-21：纯 Python 本体包内核首个实现里程碑

### 已交付范围

- 使用 Pydantic 2 定义稳定错误契约和不可变 DTO；公开 DTO 不持有 RDFLib 对象。
- 使用 YAML 清单加载版本化本体包，按固定角色解析 `core`、`domain`、`mappings`、`rules` 和 `shapes`。
- 使用 RDFLib 解析 RDF/OWL Turtle，使用 pySHACL 执行 RDFS 推理和 SHACL 约束校验。
- 把 SHACL 报告图转换为排序后的字符串型违规 DTO，避免向上层泄漏 RDF 图内部类型。
- 仅在完整解析、校验、摘要和序列化成功后原子替换当前快照；失败重载保留上一有效快照。
- SHA-256 摘要覆盖清单原始字节、固定角色名和五个本体文件的原始字节。
- 本阶段保持旁路实现，没有修改现有 Agent、前端、HiveSQL 生成或执行链路。

### 实际难点与解决方式

1. **项目声明了 editable install，但缺少多顶层包发现配置。**
   - 现象：`pip install -e ".[dev]"` 被 setuptools 拒绝，错误为 flat-layout 中发现多个顶层包。
   - 根因：初始 `pyproject.toml` 没有构建后端和显式 package discovery，README 中的安装命令实际不可复现。
   - 解决：声明 `setuptools.build_meta`，显式允许生产包并排除 `tests*`。随后 editable install 成功，安装 RDFLib 7.6.0 和 pySHACL 0.40.1。
   - 放弃：没有用“只单独安装两个新依赖”绕过项目打包问题，因为那会继续保留不可复现的标准安装流程。

2. **Pydantic 的 `frozen=True` 不能冻结嵌套字典。**
   - 现象：`PackageManifest.files[role] = value` 可以绕过模型属性冻结。
   - 解决：把公开类型收窄为 `Mapping`，验证后复制为 `MappingProxyType`，并增加序列化器和原地修改回归测试。
   - 放弃：没有只修改类型注解而继续存储普通 `dict`，因为这不能提供运行时不变性。

3. **仅检查解析后路径仍不能满足严格清单安全契约。**
   - 现象：解析后仍在包根内的 `nested/../core.ttl` 和包根内绝对路径会被接受。
   - 解决：先拒绝原始绝对路径和任何 `..` 路径段，再执行 `resolve()` 与根目录包含检查；后一道检查继续防止符号链接解析后越界。
   - 验证：新增两个修复前失败、修复后通过的边界测试。

4. **SHACL 返回 RDF 报告图，不适合作为稳定接口直接向上暴露。**
   - 解决：提取 focus node、path、message、severity 和 source shape 为字符串 DTO，并按 focus/path/message 排序，使报告可序列化且顺序稳定。

5. **快照既要支持 RDF 图消费，又不能被调用方修改。**
   - 解决：内部快照保存 N-Triples 字符串，每次调用复制方法时重新解析为新 Graph；只有候选包完全成功后才在 `RLock` 内替换引用。

6. **Windows 与多智能体执行通道存在工程流程摩擦。**
   - Windows Bash 创建进程返回 `E_ACCESSDENIED`，任务说明提取改用 PowerShell，并只写入 worktree 的 Git 内部记录目录。
   - 多个实现/审查子智能体出现长时间运行但不产出报告；采用有界等待、持久化 RED/GREEN 报告、主控制器接管和独立复审，避免把不可观察的子任务当作完成。

7. **RDF blank node 标识不能直接作为稳定 API 字段。**
   - 现象：相同输入在三个独立 Python 进程中产生三个不同的匿名 `source_shape` 字符串。
   - 解决：对 pySHACL 报告图执行 RDFLib canonicalization，给 blank node 加显式 `_:` 前缀，并使用违规 DTO 的全部字段排序。
   - 验证：修复前稳定性测试失败；修复后两个全新图的报告一致，三个独立进程输出相同 canonical shape ID。
   - 最终复审进一步发现合法 shape 可包含多个 `sh:message`，`Graph.value()` 会在独立进程中任取一个；改为收集、规范化、排序并用固定分隔符合并全部消息，同时加入三子进程自动化回归测试。

8. **重复读取文件会破坏摘要对快照的证明能力。**
   - 现象：RDF 解析后、摘要前若文件发生变化，不同 Graph 可以得到同一摘要；摘要读取和序列化失败还会泄漏原生 I/O 异常。
   - 解决：清单和五个角色文件各读取一次为不可变 bytes，RDF 解析和 SHA-256 共享同一批内容；缺失、权限和序列化失败统一映射为项目异常，失败时保留旧快照。
   - 验证：四个修复前失败的回归用例在实现后全部通过。

### 验证证据

- 本体内核测试：`28 passed`。
- 全量后端测试：`111 passed, 1 failed, 1 skipped`。唯一失败为既有乱码基线 `tests/test_ontology_relations.py::test_retrieval_returns_relations_and_defs`，不属于本阶段新增回归；另外两个已知乱码用例存在运行间波动。
- 本体内核 Ruff：`All checks passed!`。
- 本体内核 Black：`12 files would be left unchanged`。
- 全项目 Ruff：存在 292 个历史问题，其中 56 个可自动修复；本阶段没有批量修改无关历史文件。
- 全项目 Black：43 个历史文件待格式化，50 个文件无需修改；本阶段仅保证新增内核范围通过。
- 敏感标识扫描：新增生产代码和测试夹具中未发现约定禁止的真实电信标识。
- 虚拟环境仍提示无效的 `~ebsockets`、`~ip` distribution 警告；未影响本阶段验证，未在本阶段清理共享环境。

### 对应 Git 提交

- `8d01a03 feat: 定义本体内核基础契约`
- `c5ec829 fix: 冻结本体包清单映射`
- `8cef78d feat: 安全加载版本化本体包清单`
- `9abddfc fix: 拒绝本体包原始危险路径`
- `1f0ae2c feat: 使用 SHACL 校验本体图`
- `f61db69 feat: 原子发布有效本体快照`
- `9561d00 fix: 稳定 SHACL 违规标识`
- `98c8738 fix: 统一本体快照字节与错误契约`
- `e013f99 test: 覆盖本体清单序列化往返`
- `1c27e8e fix: 稳定聚合 SHACL 多消息`

### 未完成或未验证范围

- 尚未实现 Resolver、MappingRegistry、QueryPlan、Python SDK 和 REST API。
- 尚未改造现有 MySQL 元数据导入、数据库适配器、HiveSQL 编译器或其他平台编译器。
- 尚未执行大规模本体包性能基线、并发发布压力测试和跨进程快照分发验证。
- 既有乱码测试和全项目历史格式/静态检查债务仍需单独治理，不能计入本体内核已完成范围。

### 可复用经验

从风控数据分析转向 Agent 开发时，业务口径治理、数据质量校验和可追溯性经验可以直接迁移到本体工程：把口径提升为版本化语义事实，把质量规则提升为 SHACL 约束，把发布审计提升为内容摘要和失败回滚。Agent 负责理解意图，确定性的语义内核负责约束和交付边界。

## 2026-08-23：本体编辑、语义解析与 Resolver 证据记录

### 已交付范围

- 将通用词汇固定为 `urn:ontology-agent:core#`，以 `oa:Concept`、`oa:Property`、`oa:Relation`、`oa:BusinessRule`、`oa:DataSource`、`oa:PhysicalMapping` 区分领域元素与 OWL 元词汇。
- 将标记 RDF 元素解析为冻结的 Pydantic DTO，并在发布候选快照成功后原子替换语义目录；失败候选保留上一有效快照与目录。
- 交付快照绑定、只读的 Python `OntologyResolver`：URI / short name / 首选标签查询，以及 Unicode 规范化和确定性排序的概念搜索。
- 交付空白外部包初始化、校验与检查 CLI；新增通用资源不包含领域实例，真实包通过显式外部路径访问。

现有 Agent、HiveSQL 编译器、执行器、REST 和前端在本里程碑没有改动；Resolver 还没有接入现有 Agent 链路。

### 难点、根因与处理方式

1. **历史 seed 让测试看似“随机失败”。**
   - 根因：`test_retrieval_returns_relations_and_defs` 断言了已不存在的历史逻辑定义 seed，不是 UTF-8 编码损坏。
   - 处理：以 monkeypatch 提供最小内存表、关系、字段和逻辑定义，测试改为验证组装契约。该独立测试通过；当时全量结果为 `112 passed, 1 skipped`。

2. **OWL 元模型与领域元素不能混为一谈。**
   - 根因：若 SHACL 对所有 `owl:Class` 生效，`core.ttl` 的通用词汇也会被误当成业务概念；单有 `owl:Class` 又无法告诉 Resolver 哪些是领域元素。
   - 处理：领域概念同时使用 `owl:Class, oa:Concept`，属性和关系使用对应 `oa:` marker；SHACL 只 target 六个 marker。解析器只暴露带 marker 的 URI 主体。

3. **多语言显示名和检索必须跨运行稳定。**
   - 处理：首选标签按中文、无语言标签、其他语言标签分层，并在层内按文本稳定排序；Resolver 对文本使用 NFKC、trim、casefold，搜索按五档匹配等级、规范化 short name、URI 排序。Task 5 最终 review-hardening 节点的 resolver focused 为 `13 passed`，ontology-core 为 `95 passed`，全量为 `179 passed, 1 skipped`；这些是该阶段时间点的结果，不等同于后续总回归。

4. **发布不能让失败的候选污染在线读模型。**
   - 处理：锁外完成 SHACL、语义解析、序列化和候选 `OntologySnapshot` 构造，仅在成功后锁内替换；无效规则、映射或引用失败时保留旧 snapshot/catalog。Task 4 初始完成时 focused `34 passed`、全量 `143 passed, 1 skipped`；审查修复后 parser/repository focused 达到 `57 passed`，字面量副本回归通过。

5. **RDFLib 默认字面量规范化会改变 RDF 文件表达。**
   - 根因：默认 Turtle / N-Triples 重新解析可把 `"001"^^xsd:integer` 规范化为 `"1"`，破坏 lexical form。
   - 处理：包读取与快照图副本使用独立 Turtle sink，并以 `normalize=False` 构造 Literal，避免修改全局 `rdflib.NORMALIZE_LITERALS` 造成并发副作用。真实 package/repository 回归覆盖读取和公开副本两条边界。

6. **凭据规则不能只按普通 URL 处理。**
   - 根因：只按 `#` 和 `/` 取 local name 时，`urn:...:connection-string` 可绕过 denylist。
   - 处理：fragment、层级 path 与 URN 最后 `:` 段分别提取，再进行 percent decode、NFKC、casefold 和 `-` / `_` 归一化；仅对 DataSource 与 PhysicalMapping 拒绝 host、port、username、password、token、secret、connection string 等凭据谓词，避免误判普通领域属性。

7. **初始化外部目录的安全性与可用性存在真实取舍。**
   - 根因：先检查空目录再覆盖会有 TOCTOU；路径清理也可能在同名路径被替换后删除用户内容，Windows junction 还可能绕过普通目录检查。
   - 处理：在同级随机 staging 目录写入，使用独占创建、根 reparse-point/junction 检查、提交前后精确六文件字节校验与原子 rename；失败时不自动递归删除未知路径。代价是失败 staging 与已有空目标的备份需要人工确认后清理，这是 fail-closed 的有意选择。Task 6 审查修复后 focused `43 passed, 1 skipped`，最终复核时全量 `222 passed, 2 skipped`。

8. **CLI 的机器契约要区分 stdout 与 stderr。**
   - 处理：成功 JSON 写 stdout；ontology 错误 JSON 写 stderr，保持一条可解析 JSON 和稳定 `code`、`message`、`details`。检查输出只含包 ID、版本、摘要与六类计数，默认不含标识符、源路径或加载时间。Task 7 review-hardening 用真实模块进程确认 malformed package 返回 exit 1、stdout 为空且 stderr 为单个 JSON。

9. **离线 wheel 验证不能虚报为 `python -m build` 成功。**
   - 事实：当前项目虚拟环境的 `build` 前端不可作为 `python -m build --wheel` 使用。未联网安装依赖。
   - 处理与证据：使用已有 setuptools/wheel 的离线 `pip wheel --no-deps --no-build-isolation --wheel-dir dist .` 构建，并以标准库 `zipfile` 确认 wheel 包含 `ontology_core/resources/__init__.py`、`core.ttl` 和 `shapes.ttl`。这证明资源打包，但不把它表述为未执行的 build 前端命令。

### 最终阶段验证口径

- Task 6 的最终独立审查复核点：全量 `222 passed, 2 skipped`。
- Task 7 最终实现与 review-hardening 后：全量 `228 passed, 2 skipped`，CLI focused `6 passed`，Ruff、Black 与 `git diff --check` 均通过。
- 所有测试计数均为各自执行时间点的证据；没有将不同阶段的 focused 或全量数字相加，也不以它们替代后续 Task 9 的整分支验收。

### 对应提交和后续边界

- 代表性提交：`dd77da7`（移除历史 seed 依赖）、`2021ddb` / `ae5cc3c`（语义 DTO 与中性测试标识）、`6709dfb` 及语义校验修复、`06f3c3c`（原子语义目录）、`9f7fd3e`（Resolver）、`b652de9`（外部空白包）、`f9a29ac`（CLI）。
- 已交付的是本体包、解析、快照、Resolver 和 CLI 边界。`MappingRegistry`、`QueryPlan`、现有 Agent 旁路适配、REST 与前端仍是后续工作，不能写成当前成果。

## 2026-08-24：本体驱动 SQL 影子模式

### 问题与目标

本体包、语义目录和 Resolver 已经完成，但仍与 legacy Agent 并行存在，用户从聊天页面和生成 SQL 中看不到本体改造的价值。首个接入里程碑因此选择影子模式：保留现有 SQL，同时用本体快照生成独立 SQL，并在同一消息中展示差异和语义证据。

### 已交付范围

- 扩展只读 Resolver，使其按优先级提供数据源与启用的物理映射。
- 新增平台无关 `QueryPlan` 和确定性 `OntologyPlanner`；无匹配、歧义或缺失映射时拒绝猜测。
- 新增 `SqlCompiler` 协议、编译器注册表和首个 Generic SQL 编译器；自然语言规划与方言编译分离，为后续 Hive、PostgreSQL、MySQL 实现保留接口。
- 新增外部本体包运行时和 Shadow Service；本体 SQL 完全由本体映射与结构化规则生成，不读取或改写 legacy SQL。
- 在 `run_agent` 完成 legacy 输出后追加本体规划步骤；结果复用现有 trace JSON 持久化，不引入数据库迁移。
- 聊天页增加双栏 SQL、差异摘要和本体证据面板；本体侧仅允许复制，并明确标记为不会自动执行的影子预览。
- 本机脱敏演示包位于 Git 忽略目录，生产代码和提交历史不包含演示表、字段或业务规则。

### 难点与解决方式

1. **底层语义能力没有产品可见性。**
   - 处理：在现有聊天链路后增加独立影子步骤，复用 trace 作为 API 与持久化载体，前端对比展示旧 SQL、本体 SQL 和命中证据。
   - 价值：无需替换现有可执行 SQL即可验证本体是否真正影响查询结果。

2. **直接从自然语言拼 SQL 会重新引入模型幻觉和平台耦合。**
   - 处理：拆成 `OntologyPlanner -> QueryPlan -> SqlCompiler`。Planner 只能选择本体中已发布的概念、属性、规则和映射；编译器只消费结构化计划。

3. **影子链路不能拖垮已有 Agent。**
   - 处理：把 `generated/no_match/ambiguous/unsupported/unavailable` 建模为稳定结果；影子异常转为安全状态，legacy 成功状态、SQL 和 `QueryHistory` 不变。

4. **演示效果与真实效果必须区分。**
   - 处理：本机外部包只证明技术闭环；不以脱敏演示结果宣称业务准确率。真实提升将在用户测试集上按表命中率、字段命中率、规则覆盖率、可执行率和人工接受率评估。

5. **全仓静态检查存在大量历史债务。**
   - 事实：本阶段全仓 Ruff 仍报告 277 条既有问题，Black 仍识别 38 个既有待格式化文件。
   - 处理：没有借本功能批量改写无关 legacy 文件；本体内核、新增影子服务及本轮修改范围的 Ruff/Black 检查通过，并保留全量测试作为行为回归门禁。

### 验证证据

- 本机外部包 `validate` 与 `inspect` 均成功，计数覆盖概念、属性、规则、数据源和物理映射。
- 真实 `run_agent` 调用同时返回 legacy SQL 与不同的本体 SQL；本体结果包含概念、属性、规则、数据源和映射证据。
- 全量 Python 回归：`424 passed, 2 skipped`。
- 前端 TypeScript + Vite production build：通过；存在既有的大 chunk 警告，不影响构建结果。
- 本轮新增和修改范围 Ruff、Black、`git diff --check`：通过。

### 对应提交

- `60e678e feat: 增加本体查询计划器`
- `d08e777 feat: 增加可替换本体 SQL 编译器`
- `ad699f0 feat: 增加本体 SQL 影子服务`
- `0e68eb3 feat: 接入本体 SQL 影子链路`
- `e1d1755 feat: 展示本体 SQL 影子对比`

### 下一步证据

- 导入用户测试集，形成 legacy 与 ontology SQL 的批量对比基线。
- 根据失败分类扩展多表关系规划和特定数据库编译器。
- 只有在测试集证明可接受率后，才考虑让本体 SQL 从影子预览进入可执行候选。

## 2026-08-24：表格元数据生成业务本体包

### 问题与目标

此前系统已具备本体包、查询计划和影子 SQL，但缺少真实业务内容入口。直接手写 TTL 无法规模化，复用旧版 MySQL 元数据接口又会把 Python 本体内核绑定到单一数据库。本阶段增加与数据平台无关的 TSV 导入链路，使业务表和字段可以进入本体包，同时保持真实元数据不进入 Git。

### 已交付范围

- 新增冻结的表格元数据 DTO、中文列名解析、类型规范化、结构化质量诊断和本地覆盖配置。
- 新增 RDF 本体包生成器：对象转换为概念，字段转换为属性，同时生成数据源、表映射和字段映射；不生成推测性业务规则。
- 使用同级随机暂存目录完整生成和校验候选包，成功后才切换目标；失败保留上一有效包。
- 新增 `import-tabular` CLI，支持外部 TSV、物理命名空间、平台类型、方言、本地覆盖、诊断报告和显式替换。
- 查询计划器在概念名未出现时，可通过多个字段标签反推唯一所属概念；无法匹配属性时失败关闭，不再默认选择对象全部字段。
- 影子证据仅返回表映射、本次选择字段映射和规则依赖映射，避免大表把全部字段映射展示到聊天页面。

### 难点与解决方式

1. **本体引擎与本体内容容易被混为一谈。**
   - 根因：技术链路完成不等于已有业务概念、字段和口径；没有真实元数据时，日常需求必然未命中。
   - 处理：把元数据适配、质量分析、覆盖修正、本体生成和运行时加载分层。通用代码进入 Git，真实附件、覆盖、报告和生成包统一放入 `.local/`。

2. **字段描述质量不稳定，但不能让导入器擅自修正业务含义。**
   - 处理：结构错误阻断发布，空描述只警告，疑似语义冲突作为待确认项；用户确认的修正放入本地覆盖文件，不修改原附件，也不硬编码到导入器。

3. **中文字符相似度会产生大量语义冲突误报。**
   - 根因：把短文本字符 Jaccard 阈值当作语义判断，会将“代码/编号”等相关表达错误标记为冲突。
   - 处理：以真实导入报告暴露噪声后，先增加同义表达回归测试，再将规则收紧为高精度提示。复杂语义冲突留给后续有来源、置信度和人工确认的候选规则流程。

4. **新需求不应被历史 SQL 模板限制。**
   - 处理：历史需求只作为规则发现和回归证据。Planner 通过属性标签组合推断唯一概念，再由物理映射编译 SQL；本地验收使用多个未保存成 SQL 模板的字段组合，均由同一本体现场生成。

5. **大对象会放大证据模型中的无关信息。**
   - 根因：初版影子证据直接返回查询计划中的全部可用属性映射，大表会产生大量无关映射。
   - 处理：证据集合改为“对象映射 + 选择字段映射 + 已确认规则依赖映射”，并增加未使用属性不得出现在证据中的回归测试。

6. **终端 PATH 漂移会制造假基线故障。**
   - 事实：一次基线命令解析到 Python 3.7 的 Anaconda，而项目要求 Python 3.11 以上，表现为测试收集长时间无输出和标准库 API 缺失。
   - 处理：用最小 `python --version`、模块导入和单测收集定位运行时差异，随后固定使用项目虚拟环境的绝对 Python 路径。未把环境问题误修成代码问题。

### 验证证据

- 本地真实导入包通过 `validate` 和 `inspect`；概念、属性、数据源与物理映射数量和输入一致，业务规则为零。
- 用户确认的字段描述修正存在于 Git 忽略的本地覆盖及生成包中。
- 三个未保存为 SQL 模板的字段组合请求均生成目标物理表 SQL；未知属性安全返回未命中。
- 元数据解析与 CLI 回归 `27 passed`；影子、编排与聊天链路回归 `30 passed`。
- 本体内核及相关链路必要回归 `388 passed, 1 skipped`。
- 最终全量回归复跑为 `449 passed, 2 skipped`。首轮全量曾出现一次既有异步会话测试“处理被中断”，该用例独立复现通过，完整套件复跑也通过；未将其虚报为已修复，后续仍需关注后台测试共享状态的稳定性。
- 前端 TypeScript + Vite production build 通过；保留既有的大 chunk 警告。

### 对应提交

- `43282ca feat: 增加表格元数据解析与诊断`
- `f453c58 feat: 生成并安全发布元数据本体包`
- `cba33ad feat: 增加表格元数据导入命令`
- `55ecb19 feat: 通过本体属性推断查询概念`
- `fe1ba47 fix: 降低中文元数据冲突误报`
- `cedf238 fix: 精简本体影子映射证据`

## 2026-08-24：本体驱动的有界业务账期

### 问题与目标

元数据本体包已经可以让 Planner 识别表和字段，但查询仍可能扫描整张事实表；如果只在旧 Agent 的提示词里补“默认账期”，本体 SQL 本身并不会改变，也无法证明时间口径来自已确认语义。该阶段将“分区字段、粒度、默认算法、是否允许用户覆盖”建模为本体策略，并要求任何声明策略的对象只能生成带有界分区条件的 SQL。

### 已交付范围

- 新增 `TemporalPartitionPolicy`、RDF 词汇与 SHACL 约束；月表和日表分别只接受已确认的默认算法组合。
- 元数据覆盖文件可显式声明分区策略；导入器不会根据字段名或描述自动猜测真实口径。
- 新增确定性中文时间解析：支持明确月份/日期、范围、相对时间及默认推算，并区分分区时间与其他业务日期。
- `QueryPlan` 新增结构化过滤和时间决策；SQL 编译器增加安全门，声明策略后缺少时间决策或有界分区谓词即拒绝编译。
- 影子服务返回系统时间、策略来源、用户时间、最终账期和安全状态；冲突账期与无限时间范围进入澄清状态。
- 聊天页新增业务账期决策卡片，使本体改造能从 SQL 和决策证据两方面被直接观察。

### 难点与解决方式

1. **“默认账期”既是业务语义，也是防止全表扫描的安全约束。**
   - 处理：把策略放入本体包，把具体值计算放入确定性解析器，把有界性验证放入编译器；任何一层缺失都不能静默生成 SQL。

2. **不能把所有自然语言时间都当成表分区。**
   - 处理：根据当前概念的日期属性及别名判断时间归属。业务日期被识别后保留默认分区，并额外生成日期过滤；数据类型或映射未确认时失败关闭。

3. **系统时间、用户时间与本体默认值容易形成隐式优先级。**
   - 处理：优先使用无冲突的用户明确/相对时间，否则按本体策略和严格解析的系统日计算；多个冲突账期、全量历史和非法系统时间均返回稳定错误原因。

4. **底层安全优化需要让用户看得见。**
   - 处理：API 返回 `TemporalEvidence`，前端不重复计算，只展示后端决策；同时在本体证据中加入真正参与过滤的分区映射。

5. **真实业务元数据不能明文进入仓库。**
   - 处理：真实覆盖、导入报告和生成包继续保存在 `.local/`；Git 只提交通用模型、解析器、编译链路、测试和脱敏工程记录。

### 验证证据

- 本地真实月表包重新发布后包含 1 条激活时间策略，并通过 `validate` 与 `inspect`。
- 系统日为 `2026-08-24` 且需求未给时间时，本体 SQL 自动使用上一个完整自然月 `202607`；明确指定 `2026年6月` 时改用 `202606`。
- 同一需求包含两个冲突月份时返回 `clarification_required`，不生成本体 SQL。
- 服务层/编排/持久化 focused 回归 `32 passed`；规划器、编译器和时间解析 focused 回归 `46 passed`；前端 production build 通过。

### 简历与面试表述素材

- 将表级时间分区口径从提示词规则重构为“本体策略 → 确定性时间解析 → 数据库无关 QueryPlan → 方言编译器”的可解释链路。
- 通过编译期有界性安全门阻止事实表无分区扫描，并将冲突时间转为人机澄清，而不是让模型猜测。
- 使用本地忽略的真实元数据包完成端到端验收，在不泄露业务资产的前提下保留通用实现、自动化测试与可量化工程证据。

## 2026-08-27：本体导入格式契约与动态 XLSX 样例

### 问题与目标

本体包工作台已经支持 TSV、TXT 和 XLSX 导入，但页面没有说明格式，用户只能猜测列名和必填规则。真实元数据工作簿还在标准表头前放置了说明行与合并分组行，而原解析器默认第一行就是表头，导致“系统声称支持 XLSX”与实际工作格式不一致。本阶段把真实版式提炼为通用十列契约，并提供可下载、可回传的动态样例。

### 难点与解决方式

1. **展示型 Excel 与机器可解析表格的首行假设冲突。**
   - 现象：实际工作簿第一行是填写说明，第二行是对象/属性分组标题，第三行才是字段表头。
   - 处理：解析器只在每个非空工作表前 20 行中寻找唯一、完整的十列标准表头，并把表头前物理行数传入诊断定位；因此错误仍能准确指向原工作簿行号。

2. **“红色列必填”不等于每个红色单元格都必须重复填写。**
   - 现象：对象中文名和状态只在同一对象的首行填写，主键/标题大量留空；若逐行强制，参考格式自身无法通过。
   - 处理：将约束拆成列级、对象级和属性行级三层：十列必须完整存在；对象中文名/状态每对象至少一次；属性英文名/中文名/类型每行必填；主键/标题空白按否。冲突对象名称、冲突状态和未知布尔值均失败关闭，不做猜测。

3. **模板和解析器分别维护列名会产生长期漂移。**
   - 处理：新增 Python 统一格式契约，表头发现、严格校验和 XLSX 生成共同引用同一列定义。单对象和多对象模板由后端即时生成，并通过同一解析器做下载后回环测试。

4. **可下载样例不能重新把真实业务元数据写入代码。**
   - 处理：两个用户附件只用于确认版式，不进入仓库或测试。生成器仅使用 `SAMPLE_OBJECT_A` 等无业务含义占位符；模板只在内存中生成，不写入草稿、版本目录或 Git。

5. **解析错误需要可行动，同时不能泄露整行业务数据。**
   - 处理：预览诊断保留安全文件名、工作表、物理行号和列名，但不回显行内容；模板下载与导入预览完全分离，不改变草稿修订号或已选择文件。

### 验证证据

- 解析、原子导入、模板生成、API 和权限聚焦回归 `82 passed`。
- Python Ruff 检查通过；前端导入客户端契约测试通过。
- TypeScript 与 Vite production build 通过；仅保留项目既有的大 chunk 警告。
- 单对象和多对象 XLSX 均完成“动态生成 → 安全解析 → 对象/字段计数”回环验证。

### 简历与面试表述素材

- 针对企业 Excel 元数据中“说明行、合并分组行、非首行表头”的现实格式，设计了有界表头发现和物理行号诊断机制，使导入器兼顾业务可读性与机器确定性。
- 将导入规则抽象为单一 Python 契约，同时驱动严格校验和动态模板生成，避免前后端/模板格式漂移。
- 在不提交真实业务元数据的前提下，通过合成样例回环测试、原子预览和安全错误信息建立可复现的本体治理入口。

### 对应提交

- `6d02580 feat: 严格识别本体导入表格格式`
- `51a3d68 feat: 动态生成本体导入样例`
- `b05e4d0 feat: 提供本体导入样例下载接口`
- `ab2df83 feat: 增加本体导入提示与样例下载`

### 运行态验收遗漏与修正

- **现象：** 单元测试、API 测试和前端构建均通过，但用户在实际页面点击下载仍得到“样例下载失败”。
- **根因：** `8001` 端口仍由功能实现前启动的旧后端进程占用。测试进程加载了当前分支代码，而页面请求连接的是旧进程；旧进程的 OpenAPI 中不存在模板下载路由，因此返回 404。
- **漏测点：** 收尾时只检查了 `/health` 和页面是否返回 200。这只能证明服务存活，不能证明运行进程加载了当前版本，更不能证明新增交互链路可用。
- **修正：** 精确停止旧后端进程，从当前工作树重启服务；随后检查运行态 OpenAPI 包含下载路由，并在真实页面分别点击单对象、多对象下载，确认页面成功提示及后端接口 200。
- **后续规则：** 新增或修改用户可见功能后，自动化测试仍用于保证代码边界；交付前另选本次变更的一条核心用户路径做运行态验收。健康检查不再代替功能验收，也不以无关的大规模审查增加成本。

## 2026-08-27：本体草稿原子级联删除

### 问题与目标

草稿工作台此前只能新增和编辑对象、属性，无法清理错误导入或不再需要的草稿元素；对象和属性又可能被关系、关联键及时间策略引用，前端逐项删除容易产生半完成状态。本阶段增加管理员专用的删除影响预览与后端原子级联删除，并通过物理名称二次确认降低误删风险。

### 难点与解决方式

1. **删除对象并不是删除单条记录。**
   - 处理：后端先计算对象、属性、关系和时间策略的完整影响集合，再在一次草稿修订中原子提交；任一校验失败都不写入部分结果。

2. **前端确认不能替代后端安全边界。**
   - 处理：删除接口同时校验管理员角色、草稿修订号、显式级联开关和区分大小写的物理名称；已发布稳定标识继续禁止普通删除。

3. **用户需要在操作前看懂影响范围。**
   - 处理：对象和属性共用删除确认组件，先展示将删除的对象、属性、关系和时间策略数量，名称未完全匹配时不启用最终按钮。

4. **重复请求可能来自旧页面状态或重复点击。**
   - 现象：首次删除成功后，同一目标的第二次请求曾因底层 `ValueError` 冒泡为 500。
   - 处理：为缺失草稿对象增加稳定领域错误，重复删除现在返回 `draft_object_not_found`（404），不泄露内部堆栈，也不改变当前草稿。

5. **真实业务对象必须与验收数据隔离。**
   - 处理：运行态验收只删除临时合成对象及其合成引用，既有已发布对象保持不变；真实草稿和版本文件继续位于 Git 外部管理目录。

### 验证证据

- 运行态级联删除将草稿修订号从 5 增至 6，一次移除 1 个合成对象、2 个属性、1 条关系和 1 条时间策略。
- 删除后草稿保留 2 个对象、45 个属性，关系和时间策略均为 0；既有已发布对象未改变。
- 编辑器、API 与权限聚焦回归 `43 passed`，Python Ruff 检查通过。
- 前端删除客户端契约测试和 production build 通过；仅保留项目既有的大 chunk 警告。

### 简历与面试表述素材

- 为本体治理工作台设计“影响预览 → 精确名称确认 → 后端原子级联 → 修订号并发控制”的安全删除链路，覆盖对象、属性、关系和时间策略。
- 将已发布标识保护、角色权限和稳定错误协议放在服务边界，使前端体验优化不会削弱领域约束。
- 通过真实运行态核心路径发现自动化测试未覆盖的重复请求 500，并用最小回归用例将其固化为稳定 404 行为。

## 2026-08-27：关系驱动的双表本体查询

### 问题与目标

本体包已经能够管理多对象和已确认关系，但查询 Planner 仍只选择一个概念，关系无法真正改变 SQL。本阶段将已确认的对象关系编译为结构化双表计划，使包含两个明确业务属性的需求可以生成安全、可解释的等值 `INNER JOIN`，并分别应用每张表自己的时间策略。

### 难点与解决方式

1. **不能因为字段同名就猜测 JOIN。**
   - 处理：Resolver 只接受激活、已确认、具备两端关联键映射的直接关系；同优先级关系存在多个时拒绝选择，没有关系时不生成 SQL。

2. **双表 SQL 不能由模型拼接字符串。**
   - 处理：新增对象绑定、带对象别名的属性绑定和结构化关系 DTO；编译器只允许最多两个同源对象、一条等值内连接，并统一生成固定别名 `t0`、`t1`。

3. **双表不能共用一个默认账期。**
   - 处理：Planner 对每个参与对象独立读取时间策略并解析需求；在系统日 `2026-08-24` 下，月对象得到 `202607`，日对象得到 `20260822`，任一有策略对象缺少有界决策都会停止编译。

4. **升级查询计划不能破坏已有单表 SQL。**
   - 处理：单表计划也迁移到统一的元组绑定结构，但编译时不添加别名，保持既有 SQL 文本稳定；影子链路同步读取新的复数概念和时间证据。

5. **用户需要看见关系和每表账期。**
   - 处理：影子结果增加关系证据和多条时间决策，聊天页按表展示独立账期卡片，并兼容历史消息中的单条时间证据。

### 验证证据

- 直接关系解析、QueryPlan、编译器、Planner、公开 API 与影子链路聚焦回归通过；Task 11 验证 `66 passed`，Task 12 直接相关验证 `39 passed`。
- 合成双表请求生成带 `t0`/`t1` 的 `INNER JOIN`，并同时包含月分区 `202607` 与日分区 `20260822`。
- Python Ruff 检查通过；前端 production build 通过，仅保留项目既有的大 chunk 警告。
- 会话异步测试出现既有后台任务取消波动，重复运行失败位置不固定；本阶段未扩张修改无关会话调度代码。

### 简历与面试表述素材

- 将本体关系从“可管理元数据”推进为“可执行查询约束”，设计确定性直接关系解析与结构化 JOIN 编译链路，拒绝同名字段猜关联。
- 使用不可变 QueryPlan 表达双对象、对象别名、关联键和每表时间决策，实现数据库方言编译器与自然语言 Planner 解耦。
- 为双表事实查询逐表施加有界时间安全门，并在影子 SQL 中展示关系、映射及独立账期证据。

### 对应提交

- `9bbdaa4 feat: 编译唯一直接关系双表计划`
- `c5c7366 feat: 规划双表本体查询与账期证据`

## 2026-08-27：本体关系编辑本地搜索

### 问题与目标

本体对象与字段数量增长后，关系编辑依赖长下拉列表，难以快速定位关联端点；现有关系列表也缺少按标签、对象和字段筛选的能力。本阶段为关系编辑补充纯前端本地搜索，不增加后端接口，也不改变草稿数据模型。

### 难点与解决方式

1. **搜索不能只覆盖页面上最显眼的中文名称。**
   - 处理：四个端点选择器同时索引对象或字段的中文名与物理名，并采用去除首尾空白、忽略大小写的包含匹配。

2. **现有关系需要跨多个端点信息检索。**
   - 处理：将关系标签、源/目标对象中文名与物理名、源/目标字段中文名与物理名统一构造成搜索证据；端点已被删除时使用稳定占位文本，避免渲染或筛选失败。

3. **搜索交互不应产生草稿写入。**
   - 处理：所有筛选均在浏览器内完成，只有用户显式点击保存关系才会调用写接口；运行态验收通过刷新清理临时选择，并以草稿修订号不变作为无写入证据。

### 验证证据

- 合成契约覆盖大小写、空白、中文名、物理名、关系标签及缺失端点等匹配边界。
- 前端 production build 通过，仅保留项目既有的大 chunk 警告。
- 真实页面确认源/目标对象与源/目标字段均可按物理名搜索，关系列表搜索框与无关系空状态正常显示。
- 验收前后草稿修订号均为 7；当前草稿关系数为 0，因此关系列表的有数据匹配由合成契约覆盖，未为验收写入临时业务关系。

### 简历与面试表述素材

- 为本体关系治理界面设计统一搜索语义，使中文业务名称和数据库物理名称都能用于定位关系端点与既有关系。
- 将搜索规则抽取为无副作用的 TypeScript 领域函数，通过合成契约覆盖缺失引用等异常元数据，同时保持后端 API 和草稿模型稳定。

### 对应提交

- `a8aaf21 feat: 支持搜索本体关系与端点`

## 2026-08-28：理性研究台前端外壳首期重构

### 问题与目标

现有前端功能已经覆盖智能取数、本体管理、数据查询和管理后台，但整体仍接近 Ant Design 默认示例：全局缺少设计令牌，页面存在大量内联样式，导航固定高亮“对话”，登录页还直接展示默认账号形式的占位文本。一次性重构所有页面风险较高，也不利于用户快速看到效果，因此首期只处理全局主题、顶部导航、页面外壳和登录页。

### 难点与解决方式

1. **需要明显改变视觉，又不能扰动现有业务页面。**
   - 处理：建立集中式 Ant Design 主题和 `--oa-*` CSS 令牌，以暖灰白、深石墨和单一低饱和青绿形成“理性研究台”语言；首期只让既有页面继承基础色彩、字体和内容外壳，不批量改写 Chat 与本体工作台内部结构。

2. **固定导航高亮会掩盖页面位置。**
   - 根因：旧页面使用 `defaultSelectedKeys={["chat"]}`，路由切换后不会更新。
   - 处理：把导航定义和 `activeNavigationKey(pathname)` 抽成纯 TypeScript 契约，使用 React Router 当前路径驱动受控选中项，并将查询结果路由归入“数据查询”。

3. **登录体验改造不能改变认证协议。**
   - 处理：保留 `/auth/login`、token/username 存储和 `/chat` 跳转，只替换表现层；移除 `admin/admin123` 占位提示，将接口错误转为表单内可行动反馈，并补充自动填充语义和提交中状态。

4. **桌面工作台布局不能牺牲窄屏可用性。**
   - 处理：页面外壳使用受约束的内容宽度和流式边距，桌面显示完整导航，窄屏收纳为菜单；登录页由左右非对称布局折叠为表单优先的单列布局，并使用 `100dvh` 避免移动浏览器视口跳动。

5. **视觉重构容易引入不可回归的主观修改。**
   - 处理：为主题关键令牌、路由映射和登录错误映射分别增加轻量契约测试；每个可独立交付的小块通过生产构建后单独提交，既保持节奏，也便于定位回归。

### 验证与边界

- 全局主题、导航和登录错误三个 TypeScript 契约测试通过。
- 前端 TypeScript 与 Vite production build 通过；保留项目既有的大 chunk 警告，本阶段没有借视觉重构扩张到代码分包。
- 本阶段没有改变后端接口、路由地址、认证行为或任何本体业务数据。
- Chat、本体工作台、数据查询和管理页面的内部信息层级仍待后续按块改造。

### 简历与面试表述素材

- 在不重写 React/Ant Design 技术栈的前提下，将默认后台样式重构为可复用产品设计系统，并通过主题令牌兼容存量业务页面。
- 将路由状态、认证错误和视觉表现拆分为明确边界，用轻量契约测试覆盖确定性行为，降低渐进式 UI 重构对业务功能的影响。
- 采用“设计系统与外壳 → 智能取数 → 本体工作台”的分阶段策略，让每个阶段都能独立运行、独立验收和独立回滚。

## 2026-08-28：智能取数三栏证据工作台

### 问题与目标

本体影子 SQL、业务账期和语义证据已经进入聊天结果，但旧页面把回答、生成链路、两条 SQL、账期和证据全部堆在单个消息块中。用户能看到“有本体结果”，却难以在阅读 SQL 时持续核对本体究竟改变了什么。本阶段将智能取数页重构为会话目录、主要消息工作区和本体证据检查器，专门提升本体能力的可见性和可解释性。

### 难点与解决方式

1. **需要拆分 700 多行页面，又不能改动业务链路。**
   - 处理：保留 `Chat.tsx` 作为唯一业务控制器，API、轮询、发送、删除、SQL 编辑与执行副作用原样留在页面；会话目录、生成链路、消息时间线、SQL 对比和证据面板只通过 Props 与回调通信，不直接导入接口客户端。

2. **证据检查器必须知道当前展示哪条回答。**
   - 处理：新增纯函数选择规则。用户显式选择且消息仍存在时保持选择；没有选择或旧选择失效时回退到最新一条带本体结果的智能体回答；完全没有证据时返回空状态。该规则通过合成契约覆盖，避免 UI 刷新时指向不存在的消息。

3. **本体未命中不等于系统错误。**
   - 处理：将 `generated/no_match/ambiguous/unsupported/unavailable/clarification_required` 映射为稳定标题和 success/neutral/warning 语义；前端只展示后端 `summary` 和已有状态，不擅自猜测缺失口径或关系。

4. **SQL 对比与完整证据混排会造成视觉竞争。**
   - 处理：`OntologyComparison` 只保留现有 Agent SQL、本体驱动 SQL、表映射差异和操作边界；账期、概念、属性、关系、规则、数据源、映射及包版本统一进入右侧检查器。本体 SQL 继续明确标记为只读影子预览，不增加执行入口。

5. **三栏布局不能把窄屏主工作区压缩到不可用。**
   - 处理：大屏同时显示三栏；中等宽度将证据检查器切换为右侧抽屉；手机宽度进一步将会话目录切换为左侧抽屉。相同展示组件在桌面和抽屉间复用，避免两套状态和交互分叉。

6. **生成链路有价值，但不应长期抢占阅读空间。**
   - 处理：将步骤条改为默认折叠的语义化区域，摘要保留步骤数和生成状态，展开后仍可查看节点、耗时、摘要和错误状态。

### 验证与边界

- 新增证据选择与状态语义契约测试，覆盖默认最新、保持显式选择、失效回退、无证据和未命中非故障语义。
- 前端 TypeScript 与 Vite production build 通过；项目既有的大 chunk 警告仍单独保留。
- 本阶段没有修改后端 API、Agent、消息 DTO、SQL 文本、轮询频率或执行权限。
- 本体管理工作台内部仍保持原结构，作为下一块候选改造范围。

### 简历与面试表述素材

- 将“聊天气泡中附带本体信息”重构为“SQL 工作区 + 可持续核对的本体证据检查器”，提升本体驱动 Agent 的可解释性和产品可见性。
- 在不改变后端协议的前提下，用纯函数管理证据选择与失效回退，并通过契约测试保证异步消息刷新后的选择稳定性。
- 通过容器组件与纯展示组件分层，拆解大型 React 页面，同时保持会话轮询、SQL 编辑和执行链路不变。

## 2026-08-29：本体构建阶段工作台

### 问题与目标

本体管理能力已经覆盖元数据导入、对象与字段、关系、时间策略、诊断和版本发布，但旧页面将六个区域纵向堆叠。用户需要滚动整页寻找功能，也难以判断当前处于本体构建流程的哪一步。本阶段把页面改造成“状态与资产概览 → 构建阶段 → 当前操作区”的阶段式工作台，并保持所有业务接口和编辑器行为不变。

### 难点与解决方式

1. **流程导航不能变成新的后端工作流状态。**
   - 处理：阶段选择完全属于前端展示状态，不写入草稿、不改变修订号；六个阶段继续复用原业务组件和 Props，只有用户在组件内执行保存、删除、导入或发布时才调用原接口。

2. **刷新数据不能打断用户正在处理的阶段。**
   - 处理：首次成功加载时根据对象数量确定默认阶段——空草稿进入“导入元数据”，已有对象进入“对象与字段”；后续刷新只更新 overview 与版本列表，保留用户当前选择。

3. **诊断跳转需要适应从长页面滚动到单阶段切换。**
   - 处理：保留 `DiagnosticsPanel.onNavigate` 契约，将原来的 DOM 滚动行为改为切换到对象、关系或时间策略阶段，不修改诊断项结构。

4. **页面需要明显变化，但不能重写六套复杂表单。**
   - 处理：新增无业务请求的 `WorkbenchShell`，统一承担深色状态栏、资产统计、六阶段导航和活动阶段标题；`OntologyWorkbench` 继续作为唯一数据控制器，现有编辑器内部保持原样，为后续逐块重构留出清晰边界。

5. **阶段布局必须兼顾宽屏效率和窄屏可达性。**
   - 处理：桌面使用左侧构建路径与右侧操作区；低于 960px 时阶段导航转换为横向滚动列表，低于 720px 时状态和资产统计自动换行。未引入新的 UI 依赖。

### 验证与边界

- 工作台模型契约按红灯、绿灯顺序执行，覆盖空草稿默认导入、有数据默认对象、六阶段稳定顺序，以及对象、诊断和版本动态摘要。
- 前端 TypeScript 与 Vite production build 通过；仅保留项目既有的大 chunk 警告。
- 本阶段没有修改 FastAPI、本体 DTO、草稿修订协议、导入模板、关系搜索、删除、诊断处置、发布或回滚行为。
- 本阶段只完成工作台框架；对象与字段等编辑器内部视觉仍由用户验收后再决定是否分块改造。

### 简历与面试表述素材

- 将六类本体治理能力从纵向功能堆叠重构为阶段式构建工作台，用信息架构直观表达“导入—语义校准—关系—时间—诊断—发布”的本体生命周期。
- 通过纯 TypeScript 阶段模型管理默认引导和实时摘要，同时将前端导航状态与后端草稿修订状态隔离，避免 UI 重构影响本体数据一致性。
- 在不改写既有复杂表单和 API 的前提下，以容器分层和响应式导航实现可渐进交付、可独立验收的前端重构。
