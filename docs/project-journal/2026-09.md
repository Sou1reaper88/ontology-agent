# 2026-09 项目建设记录

## 本体状态在服务重启后丢失

### 现象

本体管理页面曾经能够读取草稿，但服务重启后草稿和活动运行时不可恢复，导致取数链路重新表现为本体不可用。

### 根因

管理状态曾落在临时运行目录，进程生命周期结束后没有稳定的恢复边界；就绪接口还在每次请求时尝试恢复，使启动问题与请求副作用混在一起。

### 解决方式

- 强制通过外部固定目录保存草稿、不可变版本和活动版本指针。
- 拒绝源码仓库、保留目录、系统临时目录和链接路径。
- 在 FastAPI lifespan 中只执行一次活动版本恢复。
- `/ready` 只读取运行时健康状态，并返回脱敏的稳定原因。
- 通过依赖注入保证发布器与查询链路使用同一个只读运行时快照。

### 可复用经验

智能体的本体不是普通缓存，而是生成结果所依赖的可版本化业务资产。仅实现编辑页面和内存加载不等于具备本体工程能力；还需要明确持久化位置、不可变发布、活动指针、启动恢复、快照一致性和备份边界。

### 验证方式

运行持久化路径、发布器、健康检查和管理 API 的聚焦测试；发布一个测试版本后记录版本号与摘要，重启后端并确认两者不变。记录中不保存真实表名、字段、SQL、口径或测试集内容。

## 从单条 SQL 生成迁移到可验证的多步程序

### 现象

旧链路让模型直接输出一条查询，难以表达中间结果、复杂排除和可复用步骤，也无法证明模型没有把源表当成 DDL 目标。

### 难点

- 模型需要参与业务拆解，但不能获得任意表名和 SQL 拼接权。
- 多步依赖必须保持拓扑顺序，且不能引用尚未物化的结果。
- 生成期间若本体版本变化，已规划的语义与编译时映射可能不一致。
- 旧链路仍需兼容，但不能让不受控的多语句或 DML/DDL 借回退通道进入结果。

### 解决方式

- 大模型只输出受 Pydantic 约束的意图、语义引用和步骤依赖，计划内不接受 SQL 或物理目标表字段。
- Python 绑定器完成本体解析、时间决策和 DAG 校验；只对可机器修复的引用错误允许一次定向修复。
- 确定性 Hive 编译器统一生成系统表名，每步仅允许一组 `DROP TABLE IF EXISTS` 与 `CREATE TABLE AS SELECT`，不生成末尾清理语句。
- SQLGlot 对完整脚本执行 AST 白名单校验，同时拒绝源表与系统目标同名、悬空中间表和额外语句。
- 编译前再次比对本体包 ID、版本和 SHA-256；发生变化时立即中止。
- 旧链路仅在结构化规划不可用或明确不支持时启用，且只包装恢复为单条只读查询的输出。

### 验证方式

使用虚构中性概念验证程序合同、DAG 绑定、Hive 编译、调用次数上限、快照变更中止和回退包装安全。服务冒烟检查只生成一组结果表脚本，不连接数据库执行，不记录真实需求或参考 SQL。

## 让上下文压缩跟随模型能力而不是固定常量

### 难点

上下文压缩需要在调用模型前决定是否触发，但不同模型的上下文窗口与最大输出差异很大。把 32K/4K 固定值写死会让长上下文模型过早压缩；直接切换到模型最大值又可能覆盖已有部署中更保守的输出限制。

### 解决方式

- 建立独立、不可变的模型 Token 能力档案，并为未知模型保留安全默认值。
- 使用“新显式参数、旧显式参数、模型档案”的三级优先级，保证新增能力不会破坏现有 `.env`。
- 同时考虑输入上限、总上下文窗口、实际输出预留和静态提示预留，避免只看一个数字导致请求越界。
- 当前部署已设置旧输出限制，因此接入模型档案后仍保持原有输出上限；只有未配置时才采用模型默认能力。

### 可复用经验

模型的能力上限、应用的单次生成上限和上下文管理预算是三个相关但不同的概念。Agent 平台应集中解析它们，再把同一结果提供给模型客户端和记忆压缩器，避免两个模块各自猜测模型容量。

### 验证方式

使用不含业务数据的单元测试覆盖模型缺省值、显式覆盖、旧配置兼容、未知模型回退和输入预算封顶，并运行上下文、规划器、编排器及会话接口的聚焦回归测试。

## 大规模元数据发布触发安全扫描误报

### 现象

少量对象可以完成本体校验，但导入更多对象和字段后，草稿页面没有阻断级诊断，发布阶段仍失败。

### 根因

凭据安全扫描把每个数据源和物理映射节点都计入固定的递归配置扫描预算。字段数量增长会同步增加物理映射数量，即使这些映射没有嵌套配置或凭据，仍会耗尽 1024 节点或 4096 边的预算，形成规模相关的误报。

### 解决方式

- 继续逐个检查所有数据源和物理映射根节点上的凭据型谓词，保持直接凭据检测不变。
- 只把从根节点延伸出的配置子图计入递归节点和边预算，使预算约束对应真正可能失控的嵌套遍历。
- 保留超预算时失败关闭、重复编码凭据名识别、共享配置去重和显式配置根扫描等安全边界。
- 增加超过 1024 个物理映射的回归场景，防止业务目录规模再次被误当成恶意配置深度。

### 可复用经验

安全预算应限制攻击者可控制的递归扩张，而不是限制正常业务资产总量。资源预算如果和表字段数量线性耦合，会把业务增长转化为稳定的拒绝服务条件；设计时需要区分“可信目录根的总规模”和“非结构化配置图的遍历复杂度”。

### 验证方式

运行凭据扫描专项测试，并将当前草稿构建到临时目录，确认对象、属性和映射均可完整解析。验证过程不发布版本、不切换活动指针，也不记录真实表名、字段或业务口径。

## 2026-09-04：把重复账期配置收敛为自动约定

### 难点与方案

仅在导入阶段生成时间策略，无法覆盖已经发布的旧版本；直接改写旧 RDF 则会破坏版本不可变性。本次将 `_D → P_DAY / T−2`、`_M → P_MON / 上一个完整自然月` 集中为应用规则，在加载时派生有效语义目录，保留原始包文件及摘要。新包构建和草稿展示使用同一约定，避免管理页面与取数两条链路各自解释账期。

字段必须确实存在且启用；缺失或重复映射阻断发布/查询，不虚构字段或退回全表扫描。明确账期仍覆盖默认值，补齐紧凑日账期识别，日/月运算复用日历算法，避免跨年月份直接减一出现非法值。

### 本次证据与边界

59 项相关规则、候选校验及编译测试通过；前端构建通过。只读加载当前发布版本的 19 个对象，17 个日表和 2 个月表均识别成功，未修改版本摘要。系统日期 2026-08-24 得到日账期 20260822、月账期 202607。该结果说明分区覆盖和日期计算有效，不代表业务 SQL 全部正确；本轮没有调用外部模型或执行 SQL。

## 2026-09-04：真实模型评测暴露输出预算耗尽

### 现象与定位

获得用户对 DeepSeek 数据发送的明确授权后，通过智能取数共用的 `run_agent` 入口测试真实需求，参考 SQL 仅在本地用于比较，生成脚本不执行。多条需求返回 `invalid_inferred_plan`，仅凭这个统一提示无法区分网络错误、生成截断或 JSON 合同错误。

对首条失败需求追加一次带响应摘要的诊断请求：HTTP 状态为 200，结束原因 `length`，输入 30,530 token，输出 8,192 token；输出全部计为 reasoning token，最终正文长度为 0，因此 JSON 校验报 EOF。模型列表查询也正常，客户端超时配置为 300 秒。上述证据确认这一次诊断请求的直接原因是输出预算耗尽，而非鉴权、连接或缺少时间分区；不能仅凭同一上层错误码认定其他请求的底层原因完全相同。

### 工程启示

- 需要区分模型传输失败、输出预算耗尽、正文为空和结构校验失败，避免统一提示误导用户反复补充本体。
- 应结合推理模式与结构化输出需求设计输出预算，而不是只看上下文窗口是否足够。调整前应做单例对照，不直接把默认输出额度放大到模型最大值。
- 规则/编译单测通过不代表真实模型可用；模型调用成功率与 SQL 语义正确率应分开统计。未生成 SQL 时，不能把结果描述成已经完成 SQL 正确性评测。

本轮只测试与诊断，不修改生产模型配置或业务代码。真实需求、参考 SQL、原始模型响应及测试脚本留在 Git 忽略的本地目录，不写入仓库。

最终基线结果：7 条需求均完成调用，生成 SQL 0/7，统一返回 `invalid_inferred_plan`；另进行了 1 次首条需求诊断复测。当前尚不能给出 SQL 语义正确率。

## 2026-09-04：移除客户端输出上限的单例对照

按用户要求，对首条需求保留相同元数据输入，仅在测试进程中移除请求的 `max_tokens=8192`；不改变模型、推理设置或生产配置。该入口原本没有按输入 token 截断，保留召回范围以隔离变量。省略客户端上限不意味着服务端无限额度。

结果：HTTP 200、结束原因 `stop`、输入 30,530 token、输出 18,514 token（其中推理 17,676），最终正文 2,217 字符，用时 161.23 秒。结构化 JSON 解析成功，验证了前一次预算不足确实阻止了正文产生。随后取数校验返回 `missing_candidate_join`，仍未生成 SQL。

进一步只读比对完整元数据和召回子集：主对象有 198 个字段，发送给模型的子集为 41 个；用户标识和用户状态字段在已发布本体中存在，但没有进入该次召回。模型同时指出排除订购记录需要反连接/NOT EXISTS，而候选计划及编译链路目前无法完整表达这种语义。后续应分别处理关键字段召回和计划表达能力，不能直接把失败归因于用户未补齐本体。

本轮只追加这 1 次模型对照请求，未重跑全部用例、未执行 SQL、未修改生产配置；私人报告与诊断脚本继续排除在 Git 外。

## 2026-09-04：完整字段索引与模型按需读取详情

### 改造

将字段可见性与详情预算分离：候选表保留全部字段索引，只预加载排名靠前的少量描述，并保留需求精确提及的英文/中文字段、分区和已定义关联键详情。明确点名表时优先使用这些表，未点名时继续按元数据相关性召回，原有全省表族展开仍保留。

新增模型可调用的只读字段详情工具，限定在本次召回表和同一不可变快照中；一次可查询1至64个字段，最多一轮工具请求（最多4个批量调用）后生成最终计划。查询结果同步补入校验上下文，避免模型读取了字段依据而校验器仍看到裁剪版本。越界或非法工具请求直接失败，不重试无效计划、不执行SQL，也不开放网络/文件操作工具。

候选规划工具链不再发送客户端输出 token 上限，使用提供方默认限制；其他模型调用的配置保持不变。工具续接按提供方协议保留所需上下文，不将推理正文展示为用户答案。

### 证据及未完成能力

31项相关测试通过。真实主表字段索引从41恢复为完整198项，之前遗漏的标识、状态字段可见。只对首条真实需求做一次对照：模型实际查询一轮字段详情，然后正常返回结构化计划，补齐用户标识关联及用户状态过滤，不再使用无关输出字段占位。

这次单例共两次HTTP模型调用，输入分别13,820与15,192 token，输出合计17,369 token，总计46,381 token，用时132.97秒。相比上次单例49,044 token/161.23秒有所下降，但这是单例结果，不能当作整体收益；尤其不能只用首个请求13,820 token冒充整轮消耗。

仍未生成SQL：新的阻断为日期过滤值依据校验，生成计划把日期扩展到了日初/日末时间，而当前校验按原文子串匹配。同时计划仅在未解决项中描述NOT EXISTS，当前连接结构尚不能表达反连接。后续需解决日期表达与左连接/反连接能力，本轮没有放松校验或声称业务SQL已正确。

## 2026-09-04：单例复测发现“生成成功但语义相反”

在用户完成同日日期边界改动后，仅重测首条真实需求。诊断脚本增加HTTP返回前的异常链类型记录和规划异常记录，不保存密钥。两次HTTP调用均200，先查询字段详情再正常结束；总耗时168.98秒，输入29,233 token、输出23,122 token，总计52,355 token。上次响应前异常未复现，具体原因仍未确认，不能声称网络故障已修复。

本次日期校验通过且生成了SQL，但业务验收不通过：需求要求排除订购用户，编译器固定生成INNER JOIN，实际变成筛选订购用户。模型虽在未解决项中指出需要反连接/NOT EXISTS，程序仍返回success=true和空诊断，未把关键未实现语义与普通待确认信息区分。另发现模型分区过滤与时间策略重复叠加，本例条件相容但冗余。

工程教训：字段召回、模型输出格式、值依据校验通过，不等于业务语义完整。应优先补齐计划/编译的排除运算表达及核心语义完整性检查，而非把问题归因于用户未补充本体。单例约169秒的延迟也说明取消客户端输出上限不是性能优化方案。

本轮仅测试与记录，未执行SQL、未修改生产逻辑、未重跑其他6条。详细响应及私人问题记录保存在Git忽略的.local目录；既有用户日期代码改动保持原样，未纳入本次提交。

## 2026-09-04：明确连接语义，修复排除逻辑反转

用户允许按需生成LEFT JOIN加IS NULL或NOT EXISTS。候选计划新增inner/left/anti语义及anti渲染选择；过滤区分匹配范围与连接后的WHERE。编译器将右侧业务条件与分区放入ON或相关子查询，反连接右侧不允许输出或继续关联其他对象。方向无法满足或存在未编译关联条件时拒绝生成，避免静默丢失约束。

规划器以blocking_issues报告无法表达的核心运算，校验器返回incomplete_inferred_semantics。普通待确认假设仍单独展示；这个机制依赖模型正确识别核心缺失，并非任意自然语言需求的正确性证明。

先运行6项新增测试确认旧模型不能表达上述能力，再实现修复；最终47项针对性测试通过。合成SQLite内存数据验证两种反连接的实际结果一致，覆盖双表排除、重复记录、NULL键、错误分区、普通左连接及显式WHERE空值判断，不执行任何业务SQL。

仅追加首条真实需求验收：2次HTTP均200（含一轮字段详情），模型实际选择两个anti/not_exists，SQL将两张订购表分别放入NOT EXISTS；此前“排除变筛选”的错误未再出现。耗时258.78秒，输入29,758 token、输出35,989 token，总计65,747 token。未宣称全测试集通过或数据库结果等价：仍有重复分区条件、较高延迟，以及子需求范围/地域来源假设。原始结果保存在忽略目录.local/diagnostic-rerun-20260904-164654.json。

共享后端重启的权限审核连续两次超时，未执行重启；页面仍需加载新代码后才能验收。

后续收尾：47项针对性测试再次通过，反连接修复提交为e880da5，原有日期修改未混入提交。权限审核恢复后已重启共享后端；/ready返回数据库与本体均ok，前端/chat返回200。本次收尾没有调用模型，也没有执行任何业务SQL。
