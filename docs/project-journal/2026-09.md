# 2026-09 项目建设记录

## 2026-09-10：候选规划的诊断保留与推理预算分流

### 现象

真实聊天链路中，候选元数据规划未能返回可编译计划后，旧语义规划器又可能返回“需要澄清”。此前结果仅保留后者，页面会误显示为业务口径或关联键不足，掩盖候选工具的实际失败类别；同时高强度推理会与最终结构化计划共同占用输出 token，可能导致最终正文未返回。

### 解决方式

- 当候选阶段失败、旧规划器要求澄清时，同时保留两阶段的稳定诊断和候选阶段的缺失信息；不记录模型原文、内部推理或真实需求。
- DeepSeek V4 的候选元数据规划改用 `reasoning_effort=low`，仍使用推理模式和受限字段详情工具，但减少内部推理对 JSON 正文预算的竞争。
- 仅在应用显式配置输出上限时发送 `max_tokens`；未配置时交由提供方默认值。非 DeepSeek V4 兼容提供方不会收到该专有参数。

### 验证与边界

83 项候选推断、工具续接、规划、时间解析相关测试通过；本轮已重启本地后端并确认数据库和活动本体均就绪。已完成的一次真实聊天调用只生成、未执行 SQL；它被旧澄清结果覆盖的候选阶段原因无法从历史持久化记录反推，因此没有重复调用模型消耗额度。下一次同类请求将显示完整的安全诊断。低推理强度旨在提高结构化计划的完成率和控制 token 消耗，不构成 SQL 业务正确性的证明。

## 本体状态在服务重启后丢失

### 现象

本体管理页面曾经能够读取草稿，但服务重启后草稿和活动运行时不可恢复，导致取数链路重新表现为本体不可用。

### 根因

管理状态曾落在临时运行目录，进程生命周期结束后没有稳定的恢复边界；就绪接口还在每次请求时尝试恢复，使启动问题与请求副作用混在一起。

### 解决方式

- 强制通过外部固定目录保存草稿、不可变版本和活动版本指针。
- 拒绝源码仓库、保留目录、系统临时目录和链接路径。
- 在 FastAPI lifespan 中只执行一次活动版本恢复。
- `/ready` 只读取运行时健康状态，并返回脱敏的稳定原因。
- 通过依赖注入保证发布器与查询链路使用同一个只读运行时快照。

### 可复用经验

智能体的本体不是普通缓存，而是生成结果所依赖的可版本化业务资产。仅实现编辑页面和内存加载不等于具备本体工程能力；还需要明确持久化位置、不可变发布、活动指针、启动恢复、快照一致性和备份边界。

### 验证方式

运行持久化路径、发布器、健康检查和管理 API 的聚焦测试；发布一个测试版本后记录版本号与摘要，重启后端并确认两者不变。记录中不保存真实表名、字段、SQL、口径或测试集内容。

## 从单条 SQL 生成迁移到可验证的多步程序

### 现象

旧链路让模型直接输出一条查询，难以表达中间结果、复杂排除和可复用步骤，也无法证明模型没有把源表当成 DDL 目标。

### 难点

- 模型需要参与业务拆解，但不能获得任意表名和 SQL 拼接权。
- 多步依赖必须保持拓扑顺序，且不能引用尚未物化的结果。
- 生成期间若本体版本变化，已规划的语义与编译时映射可能不一致。
- 旧链路仍需兼容，但不能让不受控的多语句或 DML/DDL 借回退通道进入结果。

### 解决方式

- 大模型只输出受 Pydantic 约束的意图、语义引用和步骤依赖，计划内不接受 SQL 或物理目标表字段。
- Python 绑定器完成本体解析、时间决策和 DAG 校验；只对可机器修复的引用错误允许一次定向修复。
- 确定性 Hive 编译器统一生成系统表名，每步仅允许一组 `DROP TABLE IF EXISTS` 与 `CREATE TABLE AS SELECT`，不生成末尾清理语句。
- SQLGlot 对完整脚本执行 AST 白名单校验，同时拒绝源表与系统目标同名、悬空中间表和额外语句。
- 编译前再次比对本体包 ID、版本和 SHA-256；发生变化时立即中止。
- 旧链路仅在结构化规划不可用或明确不支持时启用，且只包装恢复为单条只读查询的输出。

### 验证方式

使用虚构中性概念验证程序合同、DAG 绑定、Hive 编译、调用次数上限、快照变更中止和回退包装安全。服务冒烟检查只生成一组结果表脚本，不连接数据库执行，不记录真实需求或参考 SQL。

## 让上下文压缩跟随模型能力而不是固定常量

### 难点

上下文压缩需要在调用模型前决定是否触发，但不同模型的上下文窗口与最大输出差异很大。把 32K/4K 固定值写死会让长上下文模型过早压缩；直接切换到模型最大值又可能覆盖已有部署中更保守的输出限制。

### 解决方式

- 建立独立、不可变的模型 Token 能力档案，并为未知模型保留安全默认值。
- 使用“新显式参数、旧显式参数、模型档案”的三级优先级，保证新增能力不会破坏现有 `.env`。
- 同时考虑输入上限、总上下文窗口、实际输出预留和静态提示预留，避免只看一个数字导致请求越界。
- 当前部署已设置旧输出限制，因此接入模型档案后仍保持原有输出上限；只有未配置时才采用模型默认能力。

### 可复用经验

模型的能力上限、应用的单次生成上限和上下文管理预算是三个相关但不同的概念。Agent 平台应集中解析它们，再把同一结果提供给模型客户端和记忆压缩器，避免两个模块各自猜测模型容量。

### 验证方式

使用不含业务数据的单元测试覆盖模型缺省值、显式覆盖、旧配置兼容、未知模型回退和输入预算封顶，并运行上下文、规划器、编排器及会话接口的聚焦回归测试。

## 大规模元数据发布触发安全扫描误报

### 现象

少量对象可以完成本体校验，但导入更多对象和字段后，草稿页面没有阻断级诊断，发布阶段仍失败。

### 根因

凭据安全扫描把每个数据源和物理映射节点都计入固定的递归配置扫描预算。字段数量增长会同步增加物理映射数量，即使这些映射没有嵌套配置或凭据，仍会耗尽 1024 节点或 4096 边的预算，形成规模相关的误报。

### 解决方式

- 继续逐个检查所有数据源和物理映射根节点上的凭据型谓词，保持直接凭据检测不变。
- 只把从根节点延伸出的配置子图计入递归节点和边预算，使预算约束对应真正可能失控的嵌套遍历。
- 保留超预算时失败关闭、重复编码凭据名识别、共享配置去重和显式配置根扫描等安全边界。
- 增加超过 1024 个物理映射的回归场景，防止业务目录规模再次被误当成恶意配置深度。

### 可复用经验

安全预算应限制攻击者可控制的递归扩张，而不是限制正常业务资产总量。资源预算如果和表字段数量线性耦合，会把业务增长转化为稳定的拒绝服务条件；设计时需要区分“可信目录根的总规模”和“非结构化配置图的遍历复杂度”。

### 验证方式

运行凭据扫描专项测试，并将当前草稿构建到临时目录，确认对象、属性和映射均可完整解析。验证过程不发布版本、不切换活动指针，也不记录真实表名、字段或业务口径。

## 2026-09-04：把重复账期配置收敛为自动约定

### 难点与方案

仅在导入阶段生成时间策略，无法覆盖已经发布的旧版本；直接改写旧 RDF 则会破坏版本不可变性。本次将 `_D → P_DAY / T−2`、`_M → P_MON / 上一个完整自然月` 集中为应用规则，在加载时派生有效语义目录，保留原始包文件及摘要。新包构建和草稿展示使用同一约定，避免管理页面与取数两条链路各自解释账期。

字段必须确实存在且启用；缺失或重复映射阻断发布/查询，不虚构字段或退回全表扫描。明确账期仍覆盖默认值，补齐紧凑日账期识别，日/月运算复用日历算法，避免跨年月份直接减一出现非法值。

### 本次证据与边界

59 项相关规则、候选校验及编译测试通过；前端构建通过。只读加载当前发布版本的 19 个对象，17 个日表和 2 个月表均识别成功，未修改版本摘要。系统日期 2026-08-24 得到日账期 20260822、月账期 202607。该结果说明分区覆盖和日期计算有效，不代表业务 SQL 全部正确；本轮没有调用外部模型或执行 SQL。

## 2026-09-04：真实模型评测暴露输出预算耗尽

### 现象与定位

获得用户对 DeepSeek 数据发送的明确授权后，通过智能取数共用的 `run_agent` 入口测试真实需求，参考 SQL 仅在本地用于比较，生成脚本不执行。多条需求返回 `invalid_inferred_plan`，仅凭这个统一提示无法区分网络错误、生成截断或 JSON 合同错误。

对首条失败需求追加一次带响应摘要的诊断请求：HTTP 状态为 200，结束原因 `length`，输入 30,530 token，输出 8,192 token；输出全部计为 reasoning token，最终正文长度为 0，因此 JSON 校验报 EOF。模型列表查询也正常，客户端超时配置为 300 秒。上述证据确认这一次诊断请求的直接原因是输出预算耗尽，而非鉴权、连接或缺少时间分区；不能仅凭同一上层错误码认定其他请求的底层原因完全相同。

### 工程启示

- 需要区分模型传输失败、输出预算耗尽、正文为空和结构校验失败，避免统一提示误导用户反复补充本体。
- 应结合推理模式与结构化输出需求设计输出预算，而不是只看上下文窗口是否足够。调整前应做单例对照，不直接把默认输出额度放大到模型最大值。
- 规则/编译单测通过不代表真实模型可用；模型调用成功率与 SQL 语义正确率应分开统计。未生成 SQL 时，不能把结果描述成已经完成 SQL 正确性评测。

本轮只测试与诊断，不修改生产模型配置或业务代码。真实需求、参考 SQL、原始模型响应及测试脚本留在 Git 忽略的本地目录，不写入仓库。

最终基线结果：7 条需求均完成调用，生成 SQL 0/7，统一返回 `invalid_inferred_plan`；另进行了 1 次首条需求诊断复测。当前尚不能给出 SQL 语义正确率。

## 2026-09-04：移除客户端输出上限的单例对照

按用户要求，对首条需求保留相同元数据输入，仅在测试进程中移除请求的 `max_tokens=8192`；不改变模型、推理设置或生产配置。该入口原本没有按输入 token 截断，保留召回范围以隔离变量。省略客户端上限不意味着服务端无限额度。

结果：HTTP 200、结束原因 `stop`、输入 30,530 token、输出 18,514 token（其中推理 17,676），最终正文 2,217 字符，用时 161.23 秒。结构化 JSON 解析成功，验证了前一次预算不足确实阻止了正文产生。随后取数校验返回 `missing_candidate_join`，仍未生成 SQL。

进一步只读比对完整元数据和召回子集：主对象有 198 个字段，发送给模型的子集为 41 个；用户标识和用户状态字段在已发布本体中存在，但没有进入该次召回。模型同时指出排除订购记录需要反连接/NOT EXISTS，而候选计划及编译链路目前无法完整表达这种语义。后续应分别处理关键字段召回和计划表达能力，不能直接把失败归因于用户未补齐本体。

本轮只追加这 1 次模型对照请求，未重跑全部用例、未执行 SQL、未修改生产配置；私人报告与诊断脚本继续排除在 Git 外。

## 2026-09-04：完整字段索引与模型按需读取详情

### 改造

将字段可见性与详情预算分离：候选表保留全部字段索引，只预加载排名靠前的少量描述，并保留需求精确提及的英文/中文字段、分区和已定义关联键详情。明确点名表时优先使用这些表，未点名时继续按元数据相关性召回，原有全省表族展开仍保留。

新增模型可调用的只读字段详情工具，限定在本次召回表和同一不可变快照中；一次可查询1至64个字段，最多一轮工具请求（最多4个批量调用）后生成最终计划。查询结果同步补入校验上下文，避免模型读取了字段依据而校验器仍看到裁剪版本。越界或非法工具请求直接失败，不重试无效计划、不执行SQL，也不开放网络/文件操作工具。

候选规划工具链不再发送客户端输出 token 上限，使用提供方默认限制；其他模型调用的配置保持不变。工具续接按提供方协议保留所需上下文，不将推理正文展示为用户答案。

### 证据及未完成能力

31项相关测试通过。真实主表字段索引从41恢复为完整198项，之前遗漏的标识、状态字段可见。只对首条真实需求做一次对照：模型实际查询一轮字段详情，然后正常返回结构化计划，补齐用户标识关联及用户状态过滤，不再使用无关输出字段占位。

这次单例共两次HTTP模型调用，输入分别13,820与15,192 token，输出合计17,369 token，总计46,381 token，用时132.97秒。相比上次单例49,044 token/161.23秒有所下降，但这是单例结果，不能当作整体收益；尤其不能只用首个请求13,820 token冒充整轮消耗。

仍未生成SQL：新的阻断为日期过滤值依据校验，生成计划把日期扩展到了日初/日末时间，而当前校验按原文子串匹配。同时计划仅在未解决项中描述NOT EXISTS，当前连接结构尚不能表达反连接。后续需解决日期表达与左连接/反连接能力，本轮没有放松校验或声称业务SQL已正确。

## 2026-09-04：单例复测发现“生成成功但语义相反”

在用户完成同日日期边界改动后，仅重测首条真实需求。诊断脚本增加HTTP返回前的异常链类型记录和规划异常记录，不保存密钥。两次HTTP调用均200，先查询字段详情再正常结束；总耗时168.98秒，输入29,233 token、输出23,122 token，总计52,355 token。上次响应前异常未复现，具体原因仍未确认，不能声称网络故障已修复。

本次日期校验通过且生成了SQL，但业务验收不通过：需求要求排除订购用户，编译器固定生成INNER JOIN，实际变成筛选订购用户。模型虽在未解决项中指出需要反连接/NOT EXISTS，程序仍返回success=true和空诊断，未把关键未实现语义与普通待确认信息区分。另发现模型分区过滤与时间策略重复叠加，本例条件相容但冗余。

工程教训：字段召回、模型输出格式、值依据校验通过，不等于业务语义完整。应优先补齐计划/编译的排除运算表达及核心语义完整性检查，而非把问题归因于用户未补充本体。单例约169秒的延迟也说明取消客户端输出上限不是性能优化方案。

本轮仅测试与记录，未执行SQL、未修改生产逻辑、未重跑其他6条。详细响应及私人问题记录保存在Git忽略的.local目录；既有用户日期代码改动保持原样，未纳入本次提交。

## 2026-09-04：明确连接语义，修复排除逻辑反转

用户允许按需生成LEFT JOIN加IS NULL或NOT EXISTS。候选计划新增inner/left/anti语义及anti渲染选择；过滤区分匹配范围与连接后的WHERE。编译器将右侧业务条件与分区放入ON或相关子查询，反连接右侧不允许输出或继续关联其他对象。方向无法满足或存在未编译关联条件时拒绝生成，避免静默丢失约束。

规划器以blocking_issues报告无法表达的核心运算，校验器返回incomplete_inferred_semantics。普通待确认假设仍单独展示；这个机制依赖模型正确识别核心缺失，并非任意自然语言需求的正确性证明。

先运行6项新增测试确认旧模型不能表达上述能力，再实现修复；最终47项针对性测试通过。合成SQLite内存数据验证两种反连接的实际结果一致，覆盖双表排除、重复记录、NULL键、错误分区、普通左连接及显式WHERE空值判断，不执行任何业务SQL。

仅追加首条真实需求验收：2次HTTP均200（含一轮字段详情），模型实际选择两个anti/not_exists，SQL将两张订购表分别放入NOT EXISTS；此前“排除变筛选”的错误未再出现。耗时258.78秒，输入29,758 token、输出35,989 token，总计65,747 token。未宣称全测试集通过或数据库结果等价：仍有重复分区条件、较高延迟，以及子需求范围/地域来源假设。原始结果保存在忽略目录.local/diagnostic-rerun-20260904-164654.json。

共享后端重启的权限审核连续两次超时，未执行重启；页面仍需加载新代码后才能验收。

后续收尾：47项针对性测试再次通过，反连接修复提交为e880da5，原有日期修改未混入提交。权限审核恢复后已重启共享后端；/ready返回数据库与本体均ok，前端/chat返回200。本次收尾没有调用模型，也没有执行任何业务SQL。

## 2026-09-04：聊天由模型主导，SQL作为工具

问题：聊天入口每轮直接进入SQL规划，用户追问原因也被当成取数需求；历史上下文只有正文和SQL，缺少结构化失败诊断。

改造：新增conversation_agent，普通问答直接由一次模型响应完成；需要取数时由模型调用generate_sql_program，内部沿用原run_agent的本体校验/编译。工具返回后追加一次解释响应，不自动重试生成，不执行SQL、不修改本体。工具参数仅允许整合后的需求，限制一次生成，保留原SQL及trace；解释失败不丢弃已有结果。DeepSeek工具续接所需reasoning_content仅在本轮协议中回传，不作为诊断或历史持久化。

将历史trace中筛选后的诊断、缺失信息、时间依据及推断说明在原上下文预算/压缩前加入正文；不发送完整本体快照。普通回答通过conversation_response记录完成状态。还用确定性测试定位并修复轮询先读旧数据库行、再看到内存trace已清理造成的误报，以及实时trace无条件标记成功的问题。

42项针对性测试通过（提供方响应模拟，不是实际模型分流准确率评测），覆盖普通问答无工具调用、补充需求调用工具、失败解释不重试、未知工具拒绝、重复生成拒绝、SQL保留、诊断进入压缩预算、刷新/轮询状态与原SQL推断回归。本轮未执行真实业务SQL；新对话模型边界已加入测试无凭据保护，未开展付费真实模型评测。

边界：聊天API使用新对话入口；原run_agent仍是SQL生成工具，离线取数评测可继续直接测这个工具，不能把其通过率等同于聊天分流准确率。模型可以讨论历史本体依据，但没有新增在线查看/修改本体工具；若历史缺少证据，应说明未知。

追加用户确认的处理过程摘要：工具参数可附简短公开说明，经trace在对话下方展开；普通问答记录未调用SQL工具。界面明确这不是内部推理原文，不展示或持久化reasoning_content。追加摘要展示及隐私边界测试后，共43项针对性测试通过。

## 2026-09-05：补齐一次性共享提示词管理

核对发现此前仅实现上下文预算与压缩，页面仍是会话级常驻“上下文设置”，没有已确认的提示词库。新增saved_prompts表及共享CRUD：所有已登录用户可查看、新建、编辑和删除；界面每次单选一条，删除需确认。

发送消息时后端按prompt_id读取内容并复制为该后台任务的turn_prompt，提示词影响本轮对话分流、SQL工具及其内部模型调用；前端收到202后立即取消选择。后续编辑或删除模板不会改变已开始的轮次。未选择时不发送提示词；旧Conversation.context字段暂保留以兼容历史API与数据库，但不再进入模型上下文，页面入口已更名为“提示词”。

数据库迁移f93b2c14d8e7已在开发库执行。先用3项失败测试验证缺失状态，再完成实现；提示词、对话和上下文相关32项测试通过，Alembic仅有一个head。测试使用合成提示词，不调用真实模型。

## 2026-09-07：直接通过智能取数链路复测真实案例

本轮绕过尚未支持完整建表程序的评测比较器，将已有7条外部真实案例按固定系统时间、同一活动本体版本串行传入`run_agent`。测试只生成脚本，不连接数据库执行；真实需求、参考SQL和模型原始响应继续保存在Git忽略的本地报告中。

第1条以`inferred_program`生成成功，耗时约171秒，生成结果与参考SQL使用的3张源表完全一致，排除逻辑方向正确。但本地人工核对仍发现语义差异：两个允许“零值或空值”的条件被收窄为仅零值；一个日期开区间被改成闭区间；还存在重复分区条件和结果字段别名缺失。因此“安全编译成功”和“源表命中”不能等同于业务SQL正确。

第2条耗时约367秒并以`incomplete_inferred_semantics`安全拒绝。模型正确识别到跨多个账期持续满足条件需要分组聚合、集合交集或等价的多步表达，而当前候选计划合同只支持记录级过滤和关联，若强行生成会把部分账期满足的对象误纳入结果。第3条耗时约286秒并以`unsupported_join_semantics`拒绝，当前左连接/排除连接约束要求右侧对象为独立唯一叶节点，该案例的候选关联结构未满足约束。

第4至第7条最初均在约1至2秒内返回`invalid_inferred_plan`。追加一次不执行SQL的单例诊断后，DeepSeek聊天补全接口明确返回HTTP 402与余额不足；同一凭据的模型列表接口仍返回200。因此这4条不是有效的智能取数质量结果，也不能归因于本体或规划器。当前代码把提供方传输错误统一折叠为`invalid_inferred_plan`，后续应区分余额不足、限流、超时、输出截断、空正文和结构校验失败，避免给用户错误的补本体建议。

本轮有效结论是1条生成但存在业务差异、2条被安全拒绝，剩余4条因模型余额不足未完成。原始批次报告及第4条HTTP诊断保存在`.local`目录，没有进入Git。

## 2026-09-07：余额恢复后续测第4至第7条

补充余额后只续测前一轮未完成的第4至第7条，仍使用同一活动本体版本和固定系统时间，串行调用`run_agent`，只生成、不执行SQL。四条均未生成脚本，但原因已经能够与前一轮的余额错误区分。

第4条耗时约104秒，候选规划正常返回后因缺少外部用户清单、部分网络类型详单、某地市同构分表和一类补充详单对象而被`incomplete_inferred_semantics`拒绝。第6条耗时约261秒，同样因外部号码清单和某地市分表不在已发布候选目录而拒绝。第7条耗时约257秒，缺少宽带日粒度对象和外部小区清单，同时“净增”与输出存量字段之间的计算口径未定义，因此拒绝。

第5条耗时约106秒，初测返回`invalid_inferred_plan`。追加诊断显示第一次模型请求HTTP 200并返回一次字段详情工具调用，第二次工具续接请求发生`RemoteProtocolError`；这属于提供方连接/工具续接失败，不属于业务语义校验失败，也不能据此要求用户补本体。当前上层仍会把这类错误折叠为无效候选计划，后续应保留“工具续接中断”这一具体错误类型。

本轮4条均未执行SQL，也未修改本体。当前有效结果为：第4、6、7条确认存在本体覆盖或需求口径缺口，第5条需要先修复模型工具续接错误分类后再决定是否重跑。续测报告和第5条诊断响应保存在Git忽略的`.local`目录。

## 2026-09-14：动态关系证据图

难点：已发布本体关系不完整，不能让“先配置所有关系”成为生成 SQL 的门槛；同时需要区分已确认事实与模型推断，避免把同名字段自动当作业务关系。

实现：复用已发布元数据目录，以普通不可变 Python 模型保存关系来源、方向、关联字段、置信度和依据。初始图允许为空；模型或用户本轮提出的关系绑定真实字段后可进入图，但不会写回本体。已确认本体关系优先，LEFT/ANTI 不做对称去重，推断最高标为 medium。类型归一化从原校验器移到共享函数，避免维护两份规则或循环依赖。没有引入 LangChain、NetworkX 或图数据库。

验证：开发前完整基线为 925 passed、2 skipped。新增测试先以模块不存在失败，实现后关系证据、模型合同和原校验器定向回归 38 passed。此阶段尚未接入主链路，不代表真实模型 SQL 正确率已提升；未调用付费模型，也未执行业务 SQL。

## 2026-09-14：元数据聚合语义合同

直接移除旧规划器回退可能丢失聚合表达能力，因此先为候选推断增加分组和 count/sum/avg/min/max 合同，并绑定真实字段。求和与平均拒绝非数值输入，聚合输出拒绝未分组明细字段，避免安全编译后仍悄悄改变业务粒度。沿用原候选合同和校验器，没有另建规划器。

新增聚合合同测试先因类型不存在失败，实现后相关合同、校验和模型请求模拟共 37 passed。此阶段尚未切换编译器；HAVING、窗口函数及任意复杂表达式没有因此获得支持，不能据此宣称跨账期持续满足需求已全部解决。

## 2026-09-14：候选推断绑定统一关系计划

实现确定性适配器，把真实字段绑定的候选语义转换为扫描、过滤、并集物化、连接、聚合和结果建表节点；只重排可逆 INNER JOIN，LEFT/ANTI 保留方向。右侧匹配过滤和账期进入 ON/NOT EXISTS，连接后 WHERE 只引用已传播列，避免左连接被意外收窄。并集来源提前物化，内部列名消除同名关联键冲突，重复最终列名及断开的来源安全拒绝。

保留模型按需选择 LEFT JOIN IS NULL 或 NOT EXISTS，并把时间依据、推断置信度和说明传到编译证据，不新增另一条 SQL 输出。适配器测试先以模块缺失失败，适配器与统一关系模型/编译器定向回归 29 passed；反连接使用纯合成 SQLite 数据核验两种写法等价，未调用真实模型或执行业务 SQL。此阶段尚未接入服务入口。

## 2026-09-14：推断服务接入统一编译器

元数据服务现在先发送已确认关系证据，模型返回后对补充搜索范围和候选关系重新绑定，随后执行语义校验、关系计划转换、快照复核和方言编译。结果只保存同一份关系计划及证据图。空关系图可由模型补充，原来依靠字段文本重合的启发式不再阻止有依据的新链路关系；旧校验器独立调用仍保持兼容。

异常按模型提供方、结构合同、关系绑定和编译阶段分开；提供方异常不再伪装成需要用户补口径。真实数据源/字段、类型和快照检查保留。定向回归 59 passed，使用模型响应模拟，未调用付费模型；生产入口移除旧规划器自动回退仍在下一步骤。



## 2026-09-14：默认统一链路直接切换与阶段回归

按用户决策直接切换，不先影子运行。`ProgramGenerationService` 现在只调用元数据推断服务，删除自动旧规划器回退、只读查询包装与二次生成路径；`run_agent` 只由服务器 `sql_pipeline` 配置选择整次 canonical 或 legacy。评测适配器复用唯一返回脚本，不再调用本体影子服务。关系图随统一计划进入公开证据载荷；自然对话分流、一次性提示词和原有上下文传递不替换。

难点与解决：全量检查发现聊天 API 测试还注入已删除的 `allow_legacy_compatibility`，导致工具 TypeError。没有为兼容测试恢复生产回退，而是将辅助函数改成合成统一计划/脚本，更新进度和落库断言。旧图自身测试仍保留，但不再代表默认入口。涉及 `agent/metadata_inference.py`、`agent/program_generation.py`、`agent/orchestrator.py`、`tools/llm_client.py`、`ontology_core/inference_validation.py`、`ontology_core/inference_to_relational.py`、`ontology_core/relational_plan.py`、`ontology_core/relational_compiler.py`、`evaluation/generation.py` 与配置、测试和 README。

阶段定向回归 120 passed；API 辅助修正后 17 passed；最终完整回归 949 passed、2 skipped，82.02 秒。首次完整回归为 4 failed、945 passed、2 skipped，错误及修正均记录在本次提交链中。测试使用模型响应模拟与合成数据，没有调用付费 DeepSeek、没有执行业务 SQL，也未把测试通过率当作七条真实需求正确率。各独立任务已 commit/push 至 `codex/canonical-metadata-switch`；主目录 main 尚未合并，服务尚未使用这批改动。

边界：连接必须能组成可确定性编译的连接树，聚合支持分组及 count/sum/avg/min/max；任意表达式、复杂布尔口径、HAVING 和窗口运算仍未因此完整支持。评测中心目前只接入单份生成结果，多步骤语义比较仍待升级。下一阶段为请求级 LangGraph 顶层自然对话状态机与 `search_ontology` / `generate_sql_program` / `analyze_generation` 三个工具，随后用用户的七条真实需求核验前台链路。

合并续记：用户确认后将本轮开发分支快进合并到 main，并推送 GitHub。合并后的最小入口回归 47 passed；从主目录后台启动后端 8001 与前端 5199，`/health` 为 ok、`/ready` 的 database/ontology 均为 ok，`/chat` HTTP 200。日志写入 Git 忽略的 `.local/service-logs`。本轮仍未调用付费模型或执行取数 SQL，真实需求正确率待用户前台验收及后续复测。

## 2026-09-14：补回会话反馈编号

主目录切换到 main 后，原反馈编号功能没有一起集成，导致前台看不到编号；不是历史会话数据丢失。复用 `a034c35` 的六个前端文件改动，恢复会话 `C-编号` 与消息 `C-编号 / M-编号`，悬停或键盘聚焦显示、点击复制。没有修改数据库、重新编号或新增依赖；本轮只运行前端构建检查，不调用付费模型。

## 2026-09-14：C-2516 暴露问题的通用语义修复

问题不是单一沉默用户口径：推断合同缺少布尔条件树、候选表族与实际召回对象不一致、模型重述可能扩大局部修改或覆盖明确账期，失败诊断又丢失具体位置。沿用当前对话工具调用和统一编译链路，不新增框架或关键词分流。

解决：原过滤叶节点兼容扩展为 all_of/any_of/not 与 children，递归绑定真实字段和逻辑列。同源条件可提前筛选，右侧匹配留在 ON，跨对象 WHERE 整组在关联后求值；不安全的跨对象匹配拒绝，不能拆成 AND。表族一旦暴露给模型就补齐其已发布成员，候选数量上限因此为软预算，但单地市仍可选单表。Schema 错误保留安全的位置与类型，不公开模型原文、输入值、密钥或内部推理；对话失败区分 HTTP 状态、超时、网络和空响应等情况，不自动重试。

上下文修复：历史参考与本轮用户消息分开，明确本轮意图优先，助手的旧建议不作为用户已确认口径。将用户最新原文同时传给生成器，并在处理记录保存实际传入需求，以便定位局部修改偏差。明确账期从整合需求优先解析，模型时间表达只补充默认情形；同一完整范围重复列出两个端点可归一，单端点或其他冲突日期仍拒绝。

验证：先观察缺失布尔结构、诊断位置、当前消息边界和账期优先级的定向用例失败，再实现。接口回归首次 4 failed、131 passed，原因是旧模拟提供方读取旧 JSON 输入位置；更新模拟器而不恢复生产旧格式。最终本轮相关 136 passed，2.78 秒，未跑全量回归；模型响应为模拟，NULL 语义只在纯合成 SQLite 数据验证，未调用真实 DeepSeek、未执行业务 SQL。后端后台重启后 /ready 的 database/ontology 均为 ok，前端 /chat 为 HTTP 200。

边界：自然意图及局部修改准确率仍需真实模型前台验收；旧会话没有留存的具体模型校验错误无法事后重建。旧独立推断编译器不新增布尔合同支持，当前默认统一链路使用新合同与编译器。

## 2026-09-11：统一关系逻辑计划第一阶段


此前两条规划链路的表达能力并不一致：一条偏向单查询绑定，另一条能够表达部分多步骤与连接语义。第一阶段将共同底层收敛为不可变的规范关系代数合同，并增加独立的Hive编译器；本轮不替换既有入口，也不改变原有推断编译器和程序编译器。

模型只声明扫描、并集、连接、过滤、投影、聚合与物化等语义运算，不接收SQL或物理目标名。Python从同一快照绑定的候选对象和字段推导每个节点的输出列与数据类型，并按声明顺序强制拓扑、直接输入引用和列权限。这样物理表字段只来自已绑定元数据，物化目标只由系统命名器生成。

`union_all`保存与输入顺序完全一致的显式列映射，避免同构来源按位置或名称被静默错配；`left`和`anti`保存不可反转的左右方向，右侧匹配过滤留在连接条件中，而`anti`只允许输出被保留的左侧列。节点依赖和本体源表进入编译血缘，既有时间证据结构保留为空的可扩展位置。

聚焦回归实际通过32项，覆盖新关系合同与编译器以及两个既有编译器。提交号为本节所在的`feat: add canonical relational plan compiler`提交；其最终哈希由同工作树的任务报告记录，避免在提交内容中制造自引用哈希。

## 2026-09-14：直接改为模型编写 SQL 与工具调度

用户更正了C-2550分析：把无使用场景的NULL纳入零值是正确业务处理，不能作为模型擅自改错口径的证据。问题仍包括用户禁止CTE而确定性编译器生成WITH。原关系代数合同也会限制复杂表达式、窗口、HAVING等，继续扩展自定义DSL并不是最短解决路径。

按用户确认，默认改为模型直接编写Hive脚本，并调用现有本体搜索、字段详情及新增静态校验工具；Python只提供快照元数据、默认账期和校验，不改写交付SQL。无需先登记关联关系，推断假设展示在前台。禁用CTE由会话模型理解并传递，AST作最终检查；未禁止时允许CTE。NULL仍按业务解释，不设统一等零规则。

复用sqlglot作用域与字段限定检查、原程序安全校验及前台展示结构。DDL限本次临时表空间、逐步DROP/CREATE配对，无末尾清理；检查真实来源、已创建中间表、字段及基础扫描有限分区。明确指定的外部表可用，但字段未验证会记录在尚未确认项。工具有界；静态失败最多一次定向修复，提供方错误不重试。旧编译模块暂留，不作为默认或失败兜底。无新依赖，不新增影子运行。

验证：先观察新默认工厂及作者/校验功能缺失的11项失败；补充检查发现非法月份、中文邻接外部表名边界及工具记录回调3项失败后修正。最终相关61项通过，0.65秒；前端TypeScript检查通过。所有SQL为合成数据、模型响应为模拟，未调用真实DeepSeek、未执行取数SQL，不能当作七条真实需求准确率。静态检查不证明口径、JOIN方向或时间意图正确；仅在基础扫描检测分区，不推断外层过滤下推。
