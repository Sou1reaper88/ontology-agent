# 字段索引与按需详情

已获用户批准：先做字段召回，不加入校验失败后的自动修正重试，不扩展连接类型。

- [x] 先写回归：描述名额很小时仍保留所有字段索引；精确字段、分区与已知关系键保留详情；显式表名优先。
- [x] `metadata_candidates.py` 保留完整字段集合，将详情数量与索引可见性分离；增加限定本次候选集合的详情查询与上下文补全。
- [x] `tools/metadata_lookup.py` 提供只读 function tool，最多一轮工具查询后生成最终计划，不对无效计划重试；保留提供方要求的工具会话上下文。候选路径不发送客户端输出上限，其他模型调用不变。
- [x] `llm_client.py` 用紧凑索引与少量描述请求规划；`metadata_inference.py` 将实际读取的详情合并回校验输入，防止模型有证据但校验器不知道。
- [x] 定向测试、真实本体只读召回验证、首条真实模型对照；不执行SQL。工程日志随本次Git提交归档，交付时重启后端。

工具协议参考：https://api-docs.deepseek.com/guides/thinking_mode/ 。查询仅可访问当前快照、本次召回表的字段，不开放任意SQL、网络或文件访问。
