apiVersion: v1
kind: Secret
metadata:
  name: ontology-agent-secret
  namespace: ontology
type: Opaque
stringData:
  # 生产环境请用 kubectl create secret 覆盖，勿提交真实密钥
  DATABASE__PASSWORD: "ontology"
  SECRET_KEY: "change-me-in-production"
  LLM__API_KEY: "sk-xxxxxxxx"
